---
- name: Create a OSP User
  hosts: localhost
  connection: local
  gather_facts: False
  tasks:

   - name: Create OSP User
     os_user:
        state: present
        name: td-user
        password: r3dh4t!
        email: td-user@example.com
        cloud: devstack
        verify: no
     register: openstack_user
     
   - debug: 
     var: openstack_user
